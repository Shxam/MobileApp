node_modules/
build/
dist/
dist-backend/
coverage/
.DS_Store
*.log
.env*
!.env.example
firebase-service-account.json

# Local MCP config may carry API keys — the tracked .mcp.json uses ${ENV_VAR}
# expansion instead. Never commit a resolved copy.
mcp.json
.mcp.local.json

# Scratch output from the /graphify tool: machine-generated, and its manifests
# embed absolute paths from whichever machine produced them.
graphify-out/
scratch/
.claude/
.codex/
