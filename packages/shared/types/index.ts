/**
 * DEPRECATED as a definition site.
 *
 * Domain types now live in `packages/types`. This file re-exports them so
 * existing `packages/shared/types` imports keep working. Add new types to
 * `packages/types/index.ts`, never here.
 */
export * from '../../types';
