/**
 * Shared feature flags for IPL Dhaba Web / Frontend Apps.
 * Set `loyaltyAndVouchersEnabled: true` to re-enable fan points earning and voucher discount UI.
 */
export const FEATURES = {
  loyaltyAndVouchersEnabled: false,
};
