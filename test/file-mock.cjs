/**
 * Static assets resolve to their URL under Vite. Tests only ever assert on the
 * `src` attribute, so a stable stub string is enough and keeps the binary out of
 * the transform pipeline.
 */
module.exports = 'test-file-stub';
